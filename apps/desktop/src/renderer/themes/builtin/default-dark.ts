import type { Theme } from '../types';

export const defaultDark: Theme = {
  id: 'default-dark',
  name: 'Default Dark',
  type: 'dark',
  colors: {
    'perm-auto-selected-text': '#00D9C5',
  },
};
