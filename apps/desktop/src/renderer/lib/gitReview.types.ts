/**
 * Renderer-facing git-review wire DTOs.
 *
 * The concrete definitions live in shared/gitReviewWire so main, preload, and
 * renderer cannot silently drift.
 */

export type * from '../../shared/gitReviewWire';
