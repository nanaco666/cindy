{
  "name": "lizi-im",
  "version": "0.0.0",
  "private": true,
  "description": "Reusable IM transport package: WebSocket-based IM channels (feishu, ...) with credential storage, conflict detection, attachment download, and unified outbound API. Pure transport layer; hosts inject storage/IPC/path adapters.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "author": {
    "name": "Lizi",
    "email": "feedback@cindy.app"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.60.0",
    "discord.js": "^14.16.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "*",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.2.4"
  }
}
