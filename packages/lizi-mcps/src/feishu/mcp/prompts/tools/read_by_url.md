通过飞书 URL 直接读取文档/表格/知识库内容。对 docx/wiki 文档:返回文本正文 + 图片清单(available_images,每张含 index/file_token/section_hint) + 评论列表(comments);对电子表格(/sheets/* 或 wiki sheet 节点)返回单元格二维数组 values。默认 max_images=0 不下载图片,按需调 media_download 单拉,或重调本工具 max_images=20 全量下载。下载的图同时塞进 MCP image content blocks(让 Claude 看见)和 xdt_image_urls 数组(让聊天气泡展示)。✨ 评论默认包含,每条评论的 user_name 已自动解析,reply.text 里 @某人 的部分也自动替换为人名。总结文档时务必把评论一起带上。多维表格(/base/*)无图片无评论,不走该流程。
