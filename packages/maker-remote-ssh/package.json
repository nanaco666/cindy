{
  "name": "@cindy/maker-remote-ssh",
  "version": "0.0.0",
  "private": true,
  "description": "xdt-maker SSH remote: connection pool, ~/.ssh/config IO, credential resolver. Zero Electron deps. Phase A — host management only (no agent-on-remote yet).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "author": {
    "name": "Lizi",
    "email": "feedback@cindy.app"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "ssh2": "^1.16.0",
    "ssh-config": "^5.0.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "*",
    "@types/ssh2": "^1.15.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
