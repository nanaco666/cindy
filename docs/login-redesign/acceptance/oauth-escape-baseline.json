{
  "version": 1,
  "title": "Cindy 登录换肤 · 回调链 escape/security 用例基线冻结锚点(oauth-escape-baseline)",
  "frozenBy": "PR0-docs(feat/login-skin-docs)",
  "frozenAt": "2026-07-20",
  "authority": "implementation-plan.md(SHA256 aac3b349…)「分支拓扑」第1条③(b) + SC-3(v6.16:废 PR Description 自报,基线冻结为本文件)",
  "extractedFromCommit": "5995a4de253c6143bc8d8c0c996001104ffd430c",
  "selectionCriteria": "从三个回调链测试文件提取断言以下安全不变量的用例:①注入转义(escape,历史 P0 事故域:htmlLang/title/body/detail/href/label 全插值点);②anti-CSRF state 校验与回调输入校验;③浏览器授权尝试生命周期安全(取消后拒绝 late result);④凭证泄漏防护与凭证生命周期(路由不回落默认网关/登出不被回写/落盘失败不留假已连接态)。纯文案/主题/布局/模型发现形状类用例不入基线。",
  "testIdFormat": "vitest 全名路径,以「 > 」连接 describe 与 it",
  "changeControl": "只增不减;任何变更(含改名/删除)只能走带用户批准记录的单独 docs PR;PR3 新品牌分支的 providerName/detail/href/htmlLang/超长 Unicode 用例作为必需 testId 追加进本文件(implementation-plan SC-3)。check-oauth-regression-baseline.mjs(PR0a 交付)断言:基线内 testId 全部存在、非 skip/todo、本轮全部 passed,且当前 escape/security 用例总数 ≥ totalCount。 PR3 追加(2026-07-20,feat/login-skin-pr3,实现 PR 内按 SC-3「PR3 新品牌分支用例作为必需 testId 追加」授权执行,只增不减:新品牌卡全插值点 escape、providerName 注入、U-2 detail 超长 Unicode 单行、bridge seam state 不泄漏/packaged 不可达、generic 裸 done 消除,共 +6):13 → 19。",
  "files": [
    {
      "file": "apps/desktop/src/main/__tests__/oauthResultPage.test.ts",
      "testIds": [
        "renderOAuthResultPage > escapes all caller-controlled text and action fields",
        "wave4 brand login callback card (pageKind=desktop-login) > escapes all caller-controlled fields on the brand card branches",
        "wave4 brand login callback card (pageKind=desktop-login) > escapes injected provider names flowing through provider copy into the shell",
        "wave4 brand login callback card (pageKind=desktop-login) > renders detail as a single escaped error-code line (U-2), surviving long Unicode and newlines"
      ],
      "count": 4
    },
    {
      "file": "apps/desktop/src/main/__tests__/authLoopbackCallback.test.ts",
      "testIds": [
        "auth loopback callback > accepts an authorization code only for the expected state",
        "auth loopback callback > propagates provider errors after state validation",
        "auth loopback callback > rejects incomplete callbacks and ignores unrelated paths",
        "auth loopback callback page > shows the raw error code on the error page and escapes injected markup",
        "auth browser authorization cancellation slot > cancels the active attempt once",
        "auth browser authorization cancellation slot > does not let an older cleanup clear a newer attempt",
        "auth browser authorization cancellation slot > cancels post-callback work and ignores its late result",
        "auth loopback dev bridge seam (v6.13) > never exposes the anti-CSRF state through any bridge surface",
        "auth loopback dev bridge seam (v6.13) > is unreachable in packaged builds (register refused, attach/notify no-op)"
      ],
      "count": 9
    },
    {
      "file": "apps/desktop/src/main/maker-host/__tests__/genericOAuth.test.ts",
      "testIds": [
        "oauth-token 路由分支 > 有 token：Bearer 覆盖 + upstream override；cc 额外抹 x-api-key",
        "oauth-token 路由分支 > 无 token → 仍路由到本供应商上游并置哑 token（绝不回落默认路由防凭证泄漏）",
        "oauth-token 路由分支 > isHostInjectedAuthSession: oauth-token 会话视为 host 注入鉴权（codex env-key 态不落默认网关）",
        "临期刷新（单飞） > 刷新期间登出 → 不回写（撤销登出是禁止的）",
        "登录流与凭证落盘失败 > 登录时 storage.write 失败 → 硬失败且不留「已连接」内存态（回归：防重启后授权静默丢失）",
        "close() 回执路径(裸 done 消除) > code 已回、exchange 未决时取消 → 浏览器收到品牌化失败页,绝不再收裸文本 done(唯一输出路径断言)"
      ],
      "count": 6
    }
  ],
  "totalCount": 19
}
