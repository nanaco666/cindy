# 登录设计稿双盲复核验收报告

> 验收人：lead（Claude）。方法：wave1（codex gpt-5.5 xhigh，5 worker）与 wave2（codex gpt-5.6-terra xhigh，3 worker）对同一 Figma 文件（`xNK3qh7zVfrO3zrKj5tEf8`）独立盲读——wave2 禁止读取 wave1 任何产物；lead 对两波产出逐参数 diff，不一致或单源项由 lead 直连 figma MCP 读原始节点仲裁；覆盖面用 lead 早期拉取的 0:1 整页 metadata（4834 节点全量地图）核查。
>
> **结论：验收通过。** 两波独立读数在几何、色值、字体、文案、裁切参数上全部吻合；单源项经 lead 仲裁全部证实；未发现任何一处 AI 编造数值。设计稿本身的内部不一致与缺口汇总于 §5，交用户/设计拍板。

## 1. 覆盖面核查（vs 全量节点地图）

登录相关节点 100% 覆盖，无遗漏：

| 类别 | 节点数 | wave1 | wave2 | 
|---|---:|---|---|
| 桌面登录/浏览器态帧（1819×2098，国区 11 + 国际区 11） | 22 | ✓ | ✓（22/22） |
| 移动登录帧（750×1624 ×2、750×1334 ×2） | 4 | ✓ | ✓ |
| 回调卡（680×680，2 区 × 2 主题 × 3 类） | 12 | ✓ | ✓ |
| 桌面浏览器页壳（1831×1831） | 2 | ✓ | ✓ |
| 移动 Chrome 回调页（750×1623） | 4 | ✓ | ✓ |
| 组件库（input_2 / input_验证码 / log_in_button / white_button / Text_link / back / 第三方圆钮 / error_text / SSO 行 / CINDY_Client / CINDY_mobile） | 11 | ✓ | ✓（部分视觉参数因额度耗尽缺，见 §3） |
| Color System（247:1253 + 228:875 副本 + 登录语义色板 228:1042~1065） | — | ✓ | ✓（swatch fill 未暴露，lead 仲裁） |
| 分区标注框（347:409 / 347:411） | 2 | ✓ | ✓ |

节点地图中其余帧（品牌 Cover、MacOS/iPhone/iPad 商店图、CINDY_BRAND 等）不属于登录流程，不在本次范围。

## 2. 双波一致项（直接采信）

以下参数两波独立读数完全一致，作为实现基准（细节以 `figma-component-spec.md` + `DESIGN-login.md` + `verify-pass2/` 为准）：

- **色板**：品牌 `#DF0C27`、highlight `#A61629`、背景 `#2A2828`、卡片/输入框 `#312F2F`、边框 `#434343`、二级信息 `#6F6F6F`、正文 `#D4D4D4`、纯白 `#FFFFFF`；状态色 Running `#EA6B17` / Awaiting `#19D2C1` / Error `#D91F37` / Done `#2AAE5B` / info·Focus `#417CDD` / warning `#F3A115` / Auto Approval `#234DC5`
- **核心几何**：桌面帧 1819×2098；立绘 443,275,934×934；字标 570,1029,680×180（内图 100.883,8,477.232×163）；Slogan 1191,863,460×134；Log_in 570,1229,680×560（中间态 680×440）；面板 `#FBFBFB` r36；输入/按钮 540×80 r40 @ 70,158 / 70,300（gap 62）；圆钮 80×80 r50 gap70；面板→第三方 gap 40
- **组件全状态**：input_2 / input_验证码 4 态、log_in_button 5 态（normal/hover/Pressd/load/Disable 叠层与描边逐值一致）、white_button 4 态、back 3 态、Text_link 2 态、SSO 行 3 态（r60）
- **排版**：标题 HarmonyOS Sans SC Bold 32 `#252222`；副标题 Regular 20 `#6F6F6F`；输入/按钮文字 24；Global pill 70×30 `#DF0C27` r40 文字 Bold 16 白
- **回调卡**：680×680 r36；White `#FBFBFB`+`#D4D4D4` / Dark `#312F2F`+`#434343`；标题 42,352,598×38；副文案 41,396,599×23；CTA 540×80 y=529；三类立绘裁切百分比逐值一致；页壳卡位 576,226 / 移动 35,251
- **移动两档规律**：面板+控件刚性不缩放（y 973→734）；立绘 0.8×、Slogan ≈0.79×、字标 0.88× 缩放；全部文案（含全角括号的 `请输入手机号(含国家区号）`）逐字一致
- **hover 覆盖面**：hover 节点仅存在于组件库的主按钮/白按钮/back/SSO 行；页面帧内无 hover 节点；输入框/Text_link/圆钮/Global pill 无 hover 变体

## 3. 单源项仲裁记录（lead 直连 figma 三读）

wave2 因 Figma MCP 调用额度耗尽未读到、仅 wave1 单源的参数，lead 逐一仲裁：

| 项 | 仲裁方式 | 结果 |
|---|---|---|
| 浏览器等待屏按钮文案 | get_design_context `347:1378` | `取消`，参数与 wave1 一致 ✓ |
| 浏览器错误态按钮/错误文案 | get_design_context `347:1447` | `重试` / `登录服务暂不可用`，S12 参数一致 ✓ |
| 国际区验证码填完内部（input/link/back） | get_design_context `347:1852` + 整页 metadata 文本名 | `666666`/`重新发送`（270.5, 189, Regular 20 `#2A2828` underline）/back 参数一致 ✓ |
| 国际区继续中内部 + 圆钮视觉 | get_design_context `347:1585` | 圆钮 `#2A2828` + 1px `#434343` + r50、Apple 图标 48×48 居中，闭合 wave2 组件缺口 ✓ |
| 语义色板 swatch fill | get_design_context `228:1044` 抽查 | fill `#2A2828` 与图例文本一致，图例↔swatch 配对关系成立 ✓ |
| 国际区验证码三帧副标题 | 整页 metadata 文本名 ×3 | `验证码已发送至 Praise@xd.com` @ 41,75,599×23 ✓ |

**1px 描边读数偏移说明（非冲突）**：metadata 含边框外扩（CTA 71,530 / emoj 201,61 / 验证码输入 x=71），design context 给内容值（70,529 / 200,60）。实现以 design context 值为准；验证码输入 x=71 与手机/邮箱输入 x=70 的 1px 差按同一原因处理。

## 4. 验收中修正的文档错误

- `DESIGN-login.md` §0/§3 曾把国区浏览器三态标为 `347:1906/1948/1927`（实为国际区簇节点），漏登记国区簇 `347:1363/1406/1432`。两组视觉同构（pass2 逐参数证实），已修正节点归属。

## 5. 设计缺口清单（汇总，需用户/设计拍板）

**A. 设计稿内部不一致（双波独立复现，建议设计侧修稿）**
1. 移动 750×1624 立绘 y：国区 `132:2741`=116，国际区 `347:2857`=96（差 20px）——哪个为准？
2. 国际区移动帧 placeholder 仍为手机号文案（应为邮箱？桌面国际区是 `请输入邮箱`）

**B. 设计稿未提供（实现无据，需补稿或拍板）**
3. hover 缺口：输入框、Text_link、第三方圆钮、Global pill 无 hover 节点（桌面端这些控件 hover 行为无据）
4. 第三方圆钮无 pressed 节点
5. 桌面窗口外阴影 effect 参数 MCP 不暴露（两波均拒绝目测）
6. 所有文本行高仅返回 `normal`、字间距不暴露；loading 图标为静态 asset，无动画时长/easing 标注
7. Slogan 为矢量资源，无文本图层（实现按资源图直出，不排字）
8. 横屏/平板、750×1334 以下、750×1624 以上的移动端行为设计稿未定义（现有建议均标注「待用户确认」）
9. 桌面窗口极限缩小的最小适配比例数值未定义（同上）
10. `white_button/Disable` 变体存在但回调页未使用；WeChat 圆钮存在但登录帧未使用——是否保留实现？

**C. 语义确认项**
11. `login-success`/`ghost-success` 当前在 token exchange 前渲染，「登录成功」文案存在部分成功窗口（见 `callback-pages-classification.md` 改造点 6）
12. 浏览器页文案语言跟 app locale 还是浏览器 Accept-Language（改造点，现状两者并存）

## 6. 验收结论

- 参数层：**通过**。两波 + lead 三读交叉证实，无编造、无冲突。
- 覆盖层：**通过**。登录相关 53 个根节点全覆盖。
- 缺口层：12 项汇总如上，进入落地方案的「待拍板」章节，不阻塞规范产出。
