# 负向 fixture：五个禁词各埋一句未标注的旧体系执行句,扫描必须逐一命中

## 实现要求(未标注作废语境的执行句)

1. 登录页背景必须红底铺满整个视口。
2. 全部登录状态保持恒红底不变。
3. 窗口外层描边使用 2px #7A0B19。
4. 窗口内层描边使用 inset 2px #f26d7e(小写 hex 同样必须命中)。
5. Splash 失败态按白字下划线失败态交互实现。
